events {}

http {
    upstream gateway_cluster {
        server gateway:8000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://gateway_cluster;
        }
    }
}